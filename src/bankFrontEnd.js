export default class BankFrontEnd {
  constructor() {

  }

  deposit = (amount) => {
    
  };
  printStatement = () => {
	return "date || credit || debit || balance\n26/04/2022 || 1000.00 || || 1000.00";
  };
}
